import { 
Bug,
Calendar, 
Car, 
LayoutDashboard,
} from "lucide-react";

/** Keys must match the `icon` field on entries in `NAV_ROUTE_DEFS`. */
export const NAVBAR_ICON_MAP = {
  Car,
  LayoutDashboard,
  Calendar,

   
};
